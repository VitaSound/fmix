\ Placeholder test — must never actually be reached because the project
\ pins a fmix version we don't have, so `fmix test` should bail out
\ before touching this file.
cr ." dummy_test SHOULD NOT RUN" cr
1 (bye)
